// A JavaScript function is a block of code designed to perform a particular task.

function multiply(num1, num2) {
    let p = num1 * num2; // p is a local variable
    return p; // function return
}

let product = multiply(34,56) // function invocation and assigning value to a variable
console.log(product);

// () operator invokes the function